{
  "name": "cleanzup-full",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "cd cleaner && npm install && npm run build && cd ../customer && npm install && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.5.0",
    "leaflet": "^1.9.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-leaflet": "^4.2.1"
  },
  "devDependencies": {
    "@types/react": "^18.2.21",
    "@types/react-dom": "^18.2.7",
    "typescript": "^5.2.2",
    "vite": "^7.1.12"
  }
}
